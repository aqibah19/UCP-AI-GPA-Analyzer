import { o as __toESM } from "../_runtime.mjs";
import { a as scenarioAnalysis, i as round, n as computeGpa } from "./gpa-f9nDyudN.mjs";
import { i as require_jsx_runtime } from "../_libs/@radix-ui/react-label+[...].mjs";
import { b as Download, c as Printer, s as RotateCcw } from "../_libs/lucide-react.mjs";
import { n as Button } from "./router-Cv-_C-Q6.mjs";
import { t as require_jspdf_node_min } from "../_libs/jspdf.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/ResultView-BXghYS7S.js
var import_jsx_runtime = require_jsx_runtime();
var import_jspdf_node_min = /* @__PURE__ */ __toESM(require_jspdf_node_min());
var PAGE_W = 595.28;
var MARGIN = 48;
var CONTENT_W = 499.28;
var NAVY = [
	23,
	37,
	74
];
var NAVY_SOFT = [
	238,
	241,
	248
];
var RED = [
	166,
	25,
	46
];
var INK = [
	28,
	32,
	42
];
var MUTED = [
	110,
	118,
	134
];
var LINE = [
	214,
	219,
	229
];
var ZEBRA = [
	248,
	249,
	252
];
var FOOTER_TOP = 779.89;
function setFill(doc, c) {
	doc.setFillColor(c[0], c[1], c[2]);
}
function setText(doc, c) {
	doc.setTextColor(c[0], c[1], c[2]);
}
function setStroke(doc, c) {
	doc.setDrawColor(c[0], c[1], c[2]);
}
function drawPageHeader(ctx, courseName) {
	const { doc } = ctx;
	if (ctx.page === 1) return;
	setText(doc, MUTED);
	doc.setFont("helvetica", "normal");
	doc.setFontSize(8);
	doc.text("UCP AI GPA Analyzer — Academic Result Report", MARGIN, 34);
	doc.text(courseName, 547.28, 34, { align: "right" });
	setStroke(doc, LINE);
	doc.setLineWidth(.5);
	doc.line(MARGIN, 42, 547.28, 42);
}
function drawPageFooter(ctx) {
	const { doc } = ctx;
	setStroke(doc, LINE);
	doc.setLineWidth(.5);
	doc.line(MARGIN, FOOTER_TOP, 547.28, FOOTER_TOP);
	doc.setFont("helvetica", "normal");
	doc.setFontSize(7.5);
	setText(doc, MUTED);
	doc.text("Designed & Developed by Aqib Ahmed  ·  aqibah50@gmail.com", MARGIN, 793.89);
	doc.text("Disclaimer: This report is generated by an independent academic utility for UCP students.", MARGIN, 804.89);
	doc.text(`Page ${ctx.page}`, 547.28, 793.89, { align: "right" });
}
function newPage(ctx, courseName) {
	drawPageFooter(ctx);
	ctx.doc.addPage();
	ctx.page += 1;
	drawPageHeader(ctx, courseName);
	ctx.y = 66;
}
function ensure(ctx, needed, courseName) {
	if (ctx.y + needed > 767.89) newPage(ctx, courseName);
}
function sectionTitle(ctx, title, courseName) {
	ensure(ctx, 40, courseName);
	const { doc } = ctx;
	setFill(doc, RED);
	doc.rect(MARGIN, ctx.y - 8, 3, 13, "F");
	doc.setFont("helvetica", "bold");
	doc.setFontSize(11);
	setText(doc, NAVY);
	doc.text(title.toUpperCase(), 58, ctx.y + 2);
	ctx.y += 10;
	setStroke(doc, LINE);
	doc.setLineWidth(.5);
	doc.line(MARGIN, ctx.y, 547.28, ctx.y);
	ctx.y += 16;
}
/** Two-column definition grid inside a bordered card. */
function infoCard(ctx, rows, courseName) {
	const cols = 2;
	const lines = Math.ceil(rows.length / cols);
	const rowH = 24;
	const cardH = lines * rowH + 16;
	ensure(ctx, cardH + 10, courseName);
	const { doc } = ctx;
	setStroke(doc, LINE);
	setFill(doc, [
		255,
		255,
		255
	]);
	doc.setLineWidth(.7);
	doc.roundedRect(MARGIN, ctx.y, CONTENT_W, cardH, 4, 4, "FD");
	const colW = CONTENT_W / cols;
	rows.forEach((r, i) => {
		const col = i % cols;
		const line = Math.floor(i / cols);
		const x = MARGIN + col * colW + 14;
		const y = ctx.y + 20 + line * rowH;
		doc.setFont("helvetica", "normal");
		doc.setFontSize(7.5);
		setText(doc, MUTED);
		doc.text(r[0].toUpperCase(), x, y);
		doc.setFont("helvetica", "bold");
		doc.setFontSize(10);
		setText(doc, INK);
		doc.text(doc.splitTextToSize(r[1] || "—", 221.64)[0], x, y + 12);
	});
	ctx.y += cardH + 20;
}
function table(ctx, cols, rows, courseName, muteRow) {
	const { doc } = ctx;
	const headerH = 22;
	const drawHead = () => {
		ensure(ctx, 46, courseName);
		setFill(doc, NAVY);
		doc.rect(MARGIN, ctx.y, CONTENT_W, headerH, "F");
		doc.setFont("helvetica", "bold");
		doc.setFontSize(8);
		setText(doc, [
			255,
			255,
			255
		]);
		let x = MARGIN;
		cols.forEach((c) => {
			const tx = c.align === "right" ? x + c.width - 8 : x + 8;
			doc.text(c.header.toUpperCase(), tx, ctx.y + 14, { align: c.align === "right" ? "right" : "left" });
			x += c.width;
		});
		ctx.y += headerH;
	};
	drawHead();
	rows.forEach((row, ri) => {
		const cells = row.map((cell, ci) => doc.splitTextToSize(cell, (cols[ci]?.width ?? 100) - 16));
		const lineCount = Math.max(...cells.map((c) => c.length));
		const rowH = Math.max(20, lineCount * 11 + 9);
		if (ctx.y + rowH > 767.89) {
			newPage(ctx, courseName);
			drawHead();
		}
		if (ri % 2 === 1) {
			setFill(doc, ZEBRA);
			doc.rect(MARGIN, ctx.y, CONTENT_W, rowH, "F");
		}
		doc.setFont("helvetica", "normal");
		doc.setFontSize(8.5);
		setText(doc, muteRow?.(ri) ? MUTED : INK);
		let x = MARGIN;
		cells.forEach((lines, ci) => {
			const c = cols[ci];
			const tx = c.align === "right" ? x + c.width - 8 : x + 8;
			lines.forEach((ln, li) => {
				doc.text(ln, tx, ctx.y + 14 + li * 11, { align: c.align === "right" ? "right" : "left" });
			});
			x += c.width;
		});
		setStroke(doc, LINE);
		doc.setLineWidth(.4);
		doc.line(MARGIN, ctx.y + rowH, 547.28, ctx.y + rowH);
		ctx.y += rowH;
	});
	ctx.y += 22;
}
function resultBanner(ctx, result, courseName) {
	const { doc } = ctx;
	const h = 78;
	ensure(ctx, 88, courseName);
	setFill(doc, NAVY);
	doc.roundedRect(MARGIN, ctx.y, CONTENT_W, h, 5, 5, "F");
	setFill(doc, RED);
	doc.rect(MARGIN, ctx.y + h - 4, CONTENT_W, 4, "F");
	const cells = [
		["Percentage", `${round(result.percentage)}%`],
		["Grade", result.grade],
		["Grade Point", result.gradePoint.toFixed(2)],
		["GPA (Course)", result.gradePoint.toFixed(2)]
	];
	const cw = CONTENT_W / cells.length;
	cells.forEach((c, i) => {
		const cx = MARGIN + i * cw + cw / 2;
		doc.setFont("helvetica", "normal");
		doc.setFontSize(7.5);
		setText(doc, [
			190,
			199,
			218
		]);
		doc.text(c[0].toUpperCase(), cx, ctx.y + 26, { align: "center" });
		doc.setFont("helvetica", "bold");
		doc.setFontSize(20);
		setText(doc, [
			255,
			255,
			255
		]);
		doc.text(c[1], cx, ctx.y + 52, { align: "center" });
		if (i > 0) {
			setStroke(doc, [
				60,
				76,
				116
			]);
			doc.setLineWidth(.6);
			doc.line(MARGIN + i * cw, ctx.y + 16, MARGIN + i * cw, ctx.y + h - 16);
		}
	});
	ctx.y += 100;
}
function coverHeader(ctx, data) {
	const { doc } = ctx;
	const h = 96;
	setFill(doc, NAVY);
	doc.rect(0, 0, PAGE_W, h, "F");
	setFill(doc, RED);
	doc.rect(0, h, PAGE_W, 4, "F");
	setFill(doc, [
		255,
		255,
		255
	]);
	doc.roundedRect(MARGIN, 28, 44, 44, 5, 5, "F");
	doc.setFont("helvetica", "bold");
	doc.setFontSize(15);
	setText(doc, NAVY);
	doc.text("UCP", 70, 56, { align: "center" });
	doc.setFontSize(17);
	setText(doc, [
		255,
		255,
		255
	]);
	doc.text("UCP AI GPA Analyzer", 108, 48);
	doc.setFont("helvetica", "normal");
	doc.setFontSize(9.5);
	setText(doc, [
		190,
		199,
		218
	]);
	doc.text("Official-format Academic Result Report", 108, 64);
	doc.setFontSize(8);
	doc.text(`Generated: ${(/* @__PURE__ */ new Date()).toLocaleString()}`, 547.28, 48, { align: "right" });
	doc.text(data.courseType === "Lab" ? "Lab Course" : "Theory Course", 547.28, 64, { align: "right" });
	ctx.y = 136;
}
function downloadResultPdf(data, result, scenarios) {
	const doc = new import_jspdf_node_min.default({
		unit: "pt",
		format: "a4"
	});
	const ctx = {
		doc,
		y: 0,
		page: 1
	};
	const courseName = data.courseName || "Course";
	const extra = data;
	coverHeader(ctx, data);
	sectionTitle(ctx, "Student Information", courseName);
	infoCard(ctx, [["Student Name", data.studentName || "—"], ["Student ID", extra.studentId || "—"]], courseName);
	sectionTitle(ctx, "Course Information", courseName);
	infoCard(ctx, [
		["Course Name", courseName],
		["Course Code", extra.courseCode || "—"],
		["Course Type", data.courseType],
		["Semester", extra.semester || "—"],
		["Evaluation Weight Covered", `${round(result.totalWeight)}%`],
		["Categories Evaluated", String(result.categories.length)]
	], courseName);
	sectionTitle(ctx, "Result Summary", courseName);
	resultBanner(ctx, result, courseName);
	sectionTitle(ctx, "Evaluation Policy Used", courseName);
	table(ctx, [
		{
			header: "Category",
			width: CONTENT_W * .4
		},
		{
			header: "Selection Rule",
			width: CONTENT_W * .35
		},
		{
			header: "Weight",
			width: CONTENT_W * .25,
			align: "right"
		}
	], result.categories.map((c) => [
		c.name,
		ruleLabel$1(c.rule),
		`${round(c.weightPercent)}%`
	]), courseName);
	sectionTitle(ctx, "Marks Breakdown", courseName);
	const breakdownRows = [];
	const mutedRows = /* @__PURE__ */ new Set();
	for (const c of result.categories) {
		const assessments = [...c.usedItems.map((i) => `${i.label}: ${i.obtained}/${i.total}`), ...c.droppedItems.map((i) => `${i.label}: ${i.obtained}/${i.total} (dropped)`)].join("\n");
		breakdownRows.push([
			c.name,
			assessments || "—",
			`${round(c.weightPercent)}%`,
			`${round(c.scorePercent)}%`,
			`${round(c.weightedScore)}`
		]);
	}
	table(ctx, [
		{
			header: "Category",
			width: CONTENT_W * .22
		},
		{
			header: "Assessments",
			width: CONTENT_W * .38
		},
		{
			header: "Weight",
			width: CONTENT_W * .13,
			align: "right"
		},
		{
			header: "Score",
			width: CONTENT_W * .13,
			align: "right"
		},
		{
			header: "Weighted",
			width: CONTENT_W * .14,
			align: "right"
		}
	], breakdownRows, courseName, (i) => mutedRows.has(i));
	sectionTitle(ctx, "Scenario Analysis", courseName);
	table(ctx, [
		{
			header: "Scenario",
			width: CONTENT_W * .46
		},
		{
			header: "Percentage",
			width: CONTENT_W * .18,
			align: "right"
		},
		{
			header: "Grade",
			width: CONTENT_W * .18,
			align: "right"
		},
		{
			header: "Grade Point",
			width: CONTENT_W * .18,
			align: "right"
		}
	], scenarios.map((s) => [
		s.label,
		`${round(s.percentage)}%`,
		s.grade,
		s.gradePoint.toFixed(2)
	]), courseName);
	if (data.notes) {
		sectionTitle(ctx, "Notes", courseName);
		const lines = doc.splitTextToSize(data.notes, 475.28);
		ensure(ctx, lines.length * 11 + 24, courseName);
		setFill(doc, NAVY_SOFT);
		doc.roundedRect(MARGIN, ctx.y, CONTENT_W, lines.length * 11 + 20, 4, 4, "F");
		doc.setFont("helvetica", "normal");
		doc.setFontSize(8.5);
		setText(doc, INK);
		lines.forEach((ln, i) => doc.text(ln, 60, ctx.y + 16 + i * 11));
		ctx.y += lines.length * 11 + 30;
	}
	drawPageFooter(ctx);
	doc.save(`ucp-gpa-report-${courseName.replace(/[^a-z0-9]+/gi, "-").toLowerCase() || "result"}.pdf`);
}
function ruleLabel$1(rule) {
	if (rule === "all") return "All assessments (aggregate)";
	if (rule === "average") return "Average of all assessments";
	if (rule.startsWith("top")) return `Best ${rule.replace("top", "")} counted`;
	return rule;
}
function ruleLabel(rule) {
	if (rule === "all") return "All assessments (aggregate)";
	if (rule === "average") return "Average of all assessments";
	return `Best ${rule.replace("top", "")} counted`;
}
function InfoGrid({ rows }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("dl", {
		className: "grid gap-x-8 gap-y-5 p-5 sm:grid-cols-2 sm:p-6 lg:grid-cols-3",
		children: rows.map(([k, v]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "min-w-0",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("dt", {
				className: "text-[11px] font-medium uppercase tracking-wider text-muted-foreground",
				children: k
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("dd", {
				className: "mt-1 truncate text-sm font-semibold",
				children: v || "—"
			})]
		}, k))
	});
}
function Section({ title, description, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		className: "soft-card overflow-hidden",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("header", {
			className: "border-b border-border px-5 py-4 sm:px-6",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", {
				className: "text-sm font-semibold uppercase tracking-wider text-primary",
				children: title
			}), description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: "mt-1 text-xs text-muted-foreground",
				children: description
			}) : null]
		}), children]
	});
}
function StatCell({ label, value }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: "px-4 py-5 text-center sm:px-6",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
			className: "text-[11px] font-medium uppercase tracking-wider text-white/60",
			children: label
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
			className: "mt-2 font-display text-2xl font-semibold tabular-nums sm:text-3xl",
			children: value
		})]
	});
}
function ResultView({ data, onReset }) {
	const result = computeGpa(data);
	const scenarios = scenarioAnalysis(data);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: "space-y-6",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "soft-card overflow-hidden",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex flex-col gap-1 border-b border-border px-5 py-5 sm:flex-row sm:items-end sm:justify-between sm:px-6",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
							className: "text-[11px] font-medium uppercase tracking-wider text-accent",
							children: "Academic Result Report"
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h1", {
							className: "mt-1 font-display text-xl font-semibold sm:text-2xl",
							children: data.courseName || "Course"
						})] }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("p", {
							className: "text-xs text-muted-foreground",
							children: ["Generated ", (/* @__PURE__ */ new Date()).toLocaleString()]
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "hero-surface grid grid-cols-2 divide-x divide-white/10 sm:grid-cols-4",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatCell, {
								label: "Percentage",
								value: `${round(result.percentage)}%`
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatCell, {
								label: "Grade",
								value: result.grade
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatCell, {
								label: "Grade Point",
								value: result.gradePoint.toFixed(2)
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatCell, {
								label: "GPA (course)",
								value: result.gradePoint.toFixed(2)
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("p", {
						className: "border-t border-border px-5 py-3 text-xs text-muted-foreground sm:px-6",
						children: [
							"Based on ",
							round(result.totalWeight),
							"% of the course evaluation entered so far."
						]
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "no-print flex flex-wrap gap-2 sm:gap-3",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						className: "flex-1 sm:flex-none",
						onClick: () => downloadResultPdf(data, result, scenarios),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Download, { className: "mr-2 h-4 w-4" }), " Download PDF report"]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "outline",
						className: "flex-1 sm:flex-none",
						onClick: () => window.print(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Printer, { className: "mr-2 h-4 w-4" }), " Print"]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "ghost",
						className: "flex-1 sm:flex-none",
						onClick: onReset,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { className: "mr-2 h-4 w-4" }), " New analysis"]
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "grid gap-6 lg:grid-cols-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Section, {
					title: "Student Information",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InfoGrid, { rows: [["Student Name", data.studentName]] })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Section, {
					title: "Course Information",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InfoGrid, { rows: [
						["Course Name", data.courseName],
						["Course Type", data.courseType],
						["Weight covered", `${round(result.totalWeight)}%`]
					] })
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Section, {
				title: "Evaluation Policy Used",
				description: "Weightages and selection rules read from your evaluation.",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "max-h-[420px] overflow-auto",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", {
						className: "report-table min-w-[520px]",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", {
							className: "sticky top-0 z-10 bg-secondary text-left",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
									className: "px-5 py-3 text-xs font-semibold uppercase tracking-wider sm:px-6",
									children: "Category"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
									className: "px-5 py-3 text-xs font-semibold uppercase tracking-wider sm:px-6",
									children: "Selection rule"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
									className: "px-5 py-3 text-right text-xs font-semibold uppercase tracking-wider sm:px-6",
									children: "Weight"
								})
							] })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: result.categories.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", {
							className: `border-t border-border ${i % 2 ? "bg-surface" : ""}`,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-3 font-medium sm:px-6",
									children: c.name
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-3 text-muted-foreground sm:px-6",
									children: ruleLabel(c.rule)
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", {
									className: "px-5 py-3 text-right tabular-nums sm:px-6",
									children: [round(c.weightPercent), "%"]
								})
							]
						}, c.name)) })]
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Section, {
				title: "Marks Breakdown",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "max-h-[520px] overflow-auto",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", {
						className: "report-table min-w-[680px]",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", {
							className: "sticky top-0 z-10 bg-secondary text-left",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tr", { children: [
								"Category",
								"Assessments",
								"Weight",
								"Score",
								"Weighted"
							].map((h, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
								className: `px-5 py-3 text-xs font-semibold uppercase tracking-wider sm:px-6 ${i > 1 ? "text-right" : ""}`,
								children: h
							}, h)) })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: result.categories.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", {
							className: `border-t border-border align-top ${i % 2 ? "bg-surface" : ""}`,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-4 font-medium sm:px-6",
									children: c.name
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", {
									className: "px-5 py-4 text-muted-foreground sm:px-6",
									children: [c.usedItems.map((it) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "tabular-nums",
										children: [
											it.label,
											": ",
											it.obtained,
											"/",
											it.total
										]
									}, it.label + it.obtained)), c.droppedItems.map((it) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "tabular-nums line-through opacity-60",
										children: [
											it.label,
											": ",
											it.obtained,
											"/",
											it.total,
											" (dropped)"
										]
									}, "d" + it.label + it.obtained))]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", {
									className: "px-5 py-4 text-right tabular-nums sm:px-6",
									children: [round(c.weightPercent), "%"]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", {
									className: "px-5 py-4 text-right tabular-nums sm:px-6",
									children: [round(c.scorePercent), "%"]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-4 text-right font-semibold tabular-nums sm:px-6",
									children: round(c.weightedScore)
								})
							]
						}, c.name)) })]
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Section, {
				title: "Scenario Analysis",
				description: "How your final standing changes depending on the remaining evaluation weight.",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "max-h-[420px] overflow-auto",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", {
						className: "report-table min-w-[560px]",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", {
							className: "sticky top-0 z-10 bg-secondary text-left",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tr", { children: [
								"Scenario",
								"Percentage",
								"Grade",
								"Grade Point"
							].map((h, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
								className: `px-5 py-3 text-xs font-semibold uppercase tracking-wider sm:px-6 ${i > 0 ? "text-right" : ""}`,
								children: h
							}, h)) })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: scenarios.map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", {
							className: `border-t border-border ${i % 2 ? "bg-surface" : ""}`,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-3 sm:px-6",
									children: s.label
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("td", {
									className: "px-5 py-3 text-right tabular-nums sm:px-6",
									children: [round(s.percentage), "%"]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-3 text-right font-semibold sm:px-6",
									children: s.grade
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
									className: "px-5 py-3 text-right tabular-nums sm:px-6",
									children: s.gradePoint.toFixed(2)
								})
							]
						}, s.label)) })]
					})
				})
			}),
			data.notes ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("p", {
				className: "soft-card p-5 text-sm text-muted-foreground sm:p-6",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "font-semibold text-foreground",
					children: "AI notes: "
				}), data.notes]
			}) : null
		]
	});
}
//#endregion
export { ResultView };
